# V6 – schärfere Spielerinnenfotos

Es wurde KEIN neues Bild erzeugt und das Layout nicht neu gestaltet.

Geändert wurde nur die Bildauslieferung:
- Kaderfotos: statt 512×512 wird eine deutlich größere FuPa-Variante angefordert
- Trainer/Kapitäninnen: statt 256×256 wird eine größere Variante angefordert
- `srcset` ergänzt, damit Retina/HiDPI-Displays passend höher aufgelöste Bilder laden
- vorhandene Fallbacks bleiben erhalten

Damit verwendet die Seite weiterhin dieselben Spielerinnenfotos wie V5, nur mit einer
höher aufgelösten Quelle, soweit FuPa diese Variante für das jeweilige Bild bereitstellt.

Upload:
ZIP entpacken und den Inhalt in `spvgg-kaufbeuren-neu` hochladen.
Gleichnamige Dateien ersetzen und danach Cmd + Shift + R.
